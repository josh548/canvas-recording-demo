# Canvas Recording Demo
Recording the contents of a canvas using the Media Capture and Streams API

## Running
Run the commands below to build `dist/bundle.js`, then open `dist/index.html` to view the demo

```sh
npm install
npm run build
```
